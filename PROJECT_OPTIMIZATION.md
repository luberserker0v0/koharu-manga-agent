# 專案優化方案

## 📋 現況摘要

### 已實作功能
| 功能 | 狀態 | 備註 |
|------|------|------|
| 專案管理 | ✅ 完成 | 開啟/關閉/列出 |
| 圖片上傳 | ✅ 完成 | 含重複偵測 |
| LLM 模型控制 | ✅ 完成 | provider 優先 → local 備援 |
| 引擎選擇 | ✅ 完成 | 含快取（.default-engines / koharu.json） |
| 管線執行與監控 | ✅ 完成 | 含各階段摘要 |
| 品質檢查閉環 | ✅ 完成 | 提取→評估→修正→重新渲染 |
| 知識庫系統 | ✅ 完成 | 提取/建立/更新/反思 |
| Subagent 架構 | ✅ 完成 | 3 個 subagent 定義於 `.opencode/agents/` |
| 日誌系統 | ✅ 完成 | 含清理 skill |
| 智慧提醒 | ✅ 完成 | TODO_LIST 檢查 |
| 配置系統 | ✅ 完成 | 三層配置（Shared → koharu.json → CLI） |
| API 統一 | ✅ 完成 | `shared/api.js` 集中管理所有端點 |
| 測試套件 | ✅ 完成 | Jest 136 個測試，覆蓋率 96.87% |

### 已知問題
| 問題 | 影響 | 嚴重度 | 狀態 |
|------|------|--------|------|
| 無模型能力分級 | 可能選擇低品質模型 | 中 | ❌ 未實作 |
| 知識庫未自動注入翻譯流程 | 需手動載入 | 低 | ⏳ 設計完成 |
| 無重試機制 | API 失敗需手動重試 | 中 | ❌ 未實作 |

---

## 🎯 優化方案

### 階段 1：穩定性提升（高優先級）

#### 1.1 模型載入優化
- [x] 區分本地模型與 OpenAI-compatible 模型
- [x] 自動偵測模型類型並使用正確的載入方式（provider 優先 → local 備援）
- [x] 記錄於 `.default-model` 與 `koharu.json`

#### 1.2 API 格式統一
- [x] 確認 `history/apply` 的正確 payload 格式並寫入文件
- [x] 建立 `shared/api.js` 統一 API 呼叫（15+ 端點集中管理）

#### 1.3 錯誤處理強化
- [x] 增加超時設定（SSE 600s、品質檢查 300s、KB 更新 300s）
- [ ] 所有腳本增加重試機制（可設定次數）
- [ ] 增加 graceful shutdown

### 階段 2：自動化提升（中優先級）

#### 2.1 Subagent 實際整合
- [x] 主 Agent 使用 `task` tool 呼叫 subagent（設計完成）
- [x] 建立 subagent 輸入/輸出驗證（定義於 `.opencode/agents/`）
- [x] 增加 subagent 執行日誌自動記錄（`logs/` 結構）

#### 2.2 知識庫自動注入
- [x] 翻譯前自動檢查 `knowledge_base/self/` 是否存在
- [x] 若存在，自動組裝提示詞並注入 LLM（quality-checker 設計）
- [ ] 建立知識庫提示詞模板

#### 2.3 模型能力追蹤
- [ ] 建立 `model-performance.json`
- [ ] 每次翻譯後記錄品質分數
- [ ] 選擇模型時顯示歷史表現

### 階段 3：效能優化（中優先級）

#### 3.1 快取機制
- [x] 場景資料快取（減少 API 呼叫）
- [x] 引擎列表快取（`.default-engines` + `koharu.json`）
- [x] 模型目錄快取（`.default-model`）
- [x] 配置快取（`shared/config.js` 三層合併）

#### 3.2 批次處理優化
- [ ] 知識庫更新改為批次模式
- [ ] 品質檢查改為增量模式（只檢查新翻譯）

### 階段 4：使用者體驗（低優先級）

#### 4.1 互動式選單
- [ ] 模型選擇改為互動式選單（含搜尋/過濾）
- [ ] 專案選擇顯示縮圖或預覽

#### 4.2 進度視覺化
- [x] 管線進度顯示百分比與步驟（SSE 監聽已實作）
- [ ] 品質檢查進度條

#### 4.3 報告生成
- [ ] 翻譯完成後生成 HTML 報告
- [ ] 包含翻譯前後對照、品質評分、修正記錄

---

## 📊 效能基準

### 當前表現
| 指標 | 數值 | 測量日期 |
|------|------|---------|
| 管線執行時間（2 頁） | ~15 秒 | 2026-05-17 |
| 品質檢查時間（15 筆） | ~5 秒 | 2026-05-17 |
| 修正套用時間（6 筆） | ~2 秒 | 2026-05-17 |
| 匯出時間 | ~1 秒 | 2026-05-17 |
| 測試執行時間（136 個） | ~0.5 秒 | 2026-05-17 |

### 目標
| 指標 | 目標值 |
|------|--------|
| 管線執行時間 | 保持或優化 |
| 品質檢查時間 | < 10 秒（含 LLM 呼叫） |
| 整體流程時間 | < 2 分鐘（從開啟到匯出） |
| 測試覆蓋率 | > 95% |

---

## 🔮 未來功能

### 長期規劃
- [ ] 多語言支援（簡體中文、英文、韓文）
- [ ] 批量專案處理
- [ ] 雲端知識庫同步
- [ ] 翻譯風格遷移（學習其他翻譯者的風格）
- [ ] Web UI 介面
- [ ] 自動 OCR 配對優化（使用圖像特徵比對）

---

## 📝 更新記錄

| 日期 | 更新內容 |
|------|---------|
| 2026-05-17 | 初始版本，整理現況與優化方案 |
| 2026-05-17 | 更新：模型載入優化完成、API 統一完成、配置系統完成、測試套件完成 |
 